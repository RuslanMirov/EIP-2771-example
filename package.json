{
  "name": "eip2771-trusted-forwarder",
  "version": "1.0.0",
  "description": "EIP-2771 Trusted Forwarder meta-transactions — Hardhat project",
  "scripts": {
    "compile": "node scripts/compile.js",
    "test":    "npx hardhat test --config hardhat.config.js --no-compile",
    "demo":    "npx hardhat run scripts/demo.js --config hardhat.config.js --no-compile",
    "build":   "npm run compile && npm run test"
  },
  "devDependencies": {
    "hardhat":                                 "^2.28.6",
    "@nomicfoundation/hardhat-ethers":         "hh2",
    "@nomicfoundation/hardhat-chai-matchers":  "hh2",
    "ethers":  "^6.16.0",
    "chai":    "^4.5.0",
    "mocha":   "^10.8.2",
    "solc":    "^0.8.26"
  }
}